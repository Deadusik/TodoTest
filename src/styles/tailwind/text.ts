import { SPACE } from "../../utils/constants";

export const textStyles = {
    textTitle: [
        'font-medium',
        'text-2xl',
    ].join(SPACE),
    errorText: [
        'font-medium',
        'text-xl',
        'text-red-500'
    ].join(SPACE)
}